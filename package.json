{
  "name": "hahitpashtut",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "node scripts/words.mjs && npm run check && tsc && vite build && node scripts/inline.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "check": "esbuild scripts/check-play.ts --bundle --platform=node --format=esm --outfile=node_modules/.check.mjs --external:three && node node_modules/.check.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "esbuild": "^0.24.0"
  }
}