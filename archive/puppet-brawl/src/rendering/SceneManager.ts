import * as THREE from 'three';
export class SceneManager { scene=new THREE.Scene(); camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,100); renderer=new THREE.WebGLRenderer({antialias:true,preserveDrawingBuffer:true}); constructor(root:HTMLElement){ this.scene.background=new THREE.Color(0x15100c); this.renderer.setSize(innerWidth,innerHeight); this.renderer.setPixelRatio(Math.min(2,devicePixelRatio)); root.appendChild(this.renderer.domElement); this.camera.position.set(0,3.2,7); this.camera.lookAt(0,1,0); this.scene.add(new THREE.HemisphereLight(0xffe7c2,0x3b2413,2.4)); const d=new THREE.DirectionalLight(0xffffff,2); d.position.set(3,6,4); this.scene.add(d); window.addEventListener('resize',()=>{this.camera.aspect=innerWidth/innerHeight;this.camera.updateProjectionMatrix();this.renderer.setSize(innerWidth,innerHeight);}); } render(){this.renderer.render(this.scene,this.camera);} }
