import type { MarionetteController } from '../entities/MarionetteController';
import type { MatchResult } from '../game/MatchManager';
export class HUD { root=document.createElement('div'); pfill=document.createElement('div'); efill=document.createElement('div'); prompt=document.createElement('div'); result?:HTMLDivElement; onRestart=()=>{}; onLimb=(l:'leftHand'|'rightHand')=>{}; constructor(){this.root.className='hud'; this.root.innerHTML='<div class="title">PUPPET BRAWL</div><div class="topright"><button id="left">Left Wire (Q)</button><button id="right">Right Wire (E)</button><div>Drag to steer • Hold/release to snap</div></div><div class="panel"><b>Physics-first prototype</b><div>Player stamina</div><div class="bar"><div id="pbar" class="fill"></div></div><div>AI stamina</div><div class="bar"><div id="ebar" class="fill"></div></div></div><div class="prompt">↕ Pull the wooden cross, hold then release for a chaotic punch</div>'; document.body.appendChild(this.root); this.pfill=this.root.querySelector('#pbar')!; this.efill=this.root.querySelector('#ebar')!; this.prompt=this.root.querySelector('.prompt')!; this.root.querySelector<HTMLButtonElement>('#left')!.onclick=()=>this.onLimb('leftHand'); this.root.querySelector<HTMLButtonElement>('#right')!.onclick=()=>this.onLimb('rightHand');}
 update(p:MarionetteController,e:MarionetteController){this.pfill.style.width=p.state.stamina+'%'; this.efill.style.width=e.state.stamina+'%'; this.prompt.textContent=p.state.isSlack?'Puppet Slack! Let the strings recover.':'↕ Drag to move • Hold/release to snap • switch hands with buttons';}
 showResult(r:MatchResult, canShare:boolean, share:()=>void){ if(this.result) this.result.remove(); this.result=document.createElement('div'); this.result.className='result'; this.result.innerHTML=`<div class="card"><h1>${r.winner} wins!</h1><p>${r.reason}</p><button id="again">Fight again</button><button id="share">${canShare?'Share clip':'Download clip'}</button></div>`; document.body.appendChild(this.result); this.result.querySelector<HTMLButtonElement>('#again')!.onclick=()=>{this.result?.remove();this.result=undefined;this.onRestart();}; this.result.querySelector<HTMLButtonElement>('#share')!.onclick=share; } }
