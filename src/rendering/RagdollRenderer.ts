import * as THREE from 'three';
import type { Ragdoll } from '../entities/Ragdoll';
const dims: Record<string,[number,number,number]>={head:[.44,.44,.4],chest:[.64,.72,.36],hips:[.56,.44,.32],leftUpperArm:[.24,.62,.24],leftLowerArm:[.22,.56,.22],leftHand:[.26,.26,.26],rightUpperArm:[.24,.62,.24],rightLowerArm:[.22,.56,.22],rightHand:[.26,.26,.26],leftUpperLeg:[.26,.68,.26],leftLowerLeg:[.24,.64,.24],rightUpperLeg:[.26,.68,.26],rightLowerLeg:[.24,.64,.24]};
export class RagdollRenderer { meshes=new Map<string,THREE.Mesh>(); group=new THREE.Group(); constructor(scene:THREE.Scene, private ragdoll:Ragdoll){ const mat=new THREE.MeshStandardMaterial({color:ragdoll.color,roughness:.85}); const jointMat=new THREE.MeshStandardMaterial({color:0x4a2d18,metalness:.1}); for(const [name] of ragdoll.bodies){ const geo=new THREE.BoxGeometry(...dims[name]); const mesh=new THREE.Mesh(geo,mat); this.meshes.set(name,mesh); this.group.add(mesh); const bolt=new THREE.Mesh(new THREE.SphereGeometry(.08,8,8),jointMat); mesh.add(bolt);} scene.add(this.group);} update(){ for(const [n,b] of this.ragdoll.bodies){ const m=this.meshes.get(n)!; const p=b.translation(), r=b.rotation(); m.position.set(p.x,p.y,p.z); m.quaternion.set(r.x,r.y,r.z,r.w);} } }
