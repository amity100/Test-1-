import type { BodySnapshot } from '../core/PhysicsWorld';
export type ReplayFrame={time:number;bodies:BodySnapshot[]};
export class ClipRecorder { private frames:ReplayFrame[]=[]; constructor(private seconds=8){} record(time:number,bodies:BodySnapshot[]){this.frames.push({time,bodies}); const min=time-this.seconds; while(this.frames[0]&&this.frames[0].time<min)this.frames.shift();} getClip(){return this.frames.slice();} async exportCanvas(canvas:HTMLCanvasElement){ const stream=canvas.captureStream(30); const chunks:Blob[]=[]; const rec=new MediaRecorder(stream,{mimeType:'video/webm'}); rec.ondataavailable=e=>chunks.push(e.data); rec.start(); await new Promise(r=>setTimeout(r,3000)); rec.stop(); await new Promise(r=>rec.onstop=r); return new Blob(chunks,{type:'video/webm'});} }
